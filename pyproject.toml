[project]
name = "appcc-yeva"
version = "0.1.0"
description = "Plan APPCC (HACCP) digitalizado para la empresa ficticia Yeva"
authors = [{name = "Yeva"}]
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
appcc-yeva = "appcc_yeva.cli:main"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
